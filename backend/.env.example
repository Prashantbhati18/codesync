# Server
PORT=5000
NODE_ENV=development

# Database (Railway MySQL)
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=codesync

# JWT
JWT_SECRET=Prashant_CodeSync_JWT_Secret_2026
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=Prashant_CodeSync_Refresh_Secret_2026
JWT_REFRESH_EXPIRES_IN=30d

# Email (Nodemailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=CodeSync <no-reply@codesync.com>

# Frontend URL (for reset password links, CORS)
CLIENT_URL=http://localhost:5173
